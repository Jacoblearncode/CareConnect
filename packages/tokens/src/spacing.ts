/** Base spacing scale, unaffected by Accessibility Mode — only type, touch targets, contrast, and motion change (§3.5). */
export const SPACING = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
} as const;
