// Design tokens, including the Senior/Accessibility Mode scale (Build Plan §3.5).
// Populated in Phase 6 — Accessibility. Left as a package boundary now so the
// mobile app never has a reason to hardcode a spacing or type-scale value.
export {};
