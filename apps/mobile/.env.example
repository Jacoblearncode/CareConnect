# Copy to .env. EXPO_PUBLIC_-prefixed vars are inlined into the client
# bundle at build time (Expo's own convention) — there is nothing secret
# here, just where to find the API during local development.
EXPO_PUBLIC_API_URL=http://localhost:8787
